---
import "@fontsource/google-sans-flex";
import Watermark from "../components/Watermark.astro";
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="generator" content={Astro.generator} />
		<meta
			name="description"
			content="NextWBC white blood cell classification dashboard."
		/>
		<title>NextWBC</title>

		<style is:global>
			/* ===========================
			   BLACKHOLE TOKEN SYSTEM
			=========================== */
			:root {
				--hue: 198;
				--bh-void: #ffffff;
				--bh-deep: rgba(224, 242, 254, 0.5);
				--bh-surface: rgba(255, 255, 255, 0.65);
				--bh-card: rgba(255, 255, 255, 0.65);
				--bh-border: rgba(255, 255, 255, 0.5);
				--bh-border-hi: rgba(255, 255, 255, 0.85);
				--bh-text: #0f172a;
				--bh-muted: #334155;
				--bh-accent: #0284c7; /* Aero Sky Blue */
				--bh-accent2: #38bdf8; /* Aero Aqua */
				--bh-accent3: #10b981; /* Emerald Green */
				--bh-danger: #ef4444;
				--bh-glow: rgba(14, 165, 233, 0.3);
				--bh-glow-sm: rgba(56, 189, 248, 0.2);
			}

			/* ===========================
			   BASE
			=========================== */
			html,
			body {
				width: 100%;
				height: 100vh;
				overflow: hidden;
				background: transparent;
				font-family:
					"Google Sans Flex", "Google Sans", "Helvetica Neue",
					sans-serif;
				color: var(--bh-text);
				line-height: 1.5;
				margin: 0;
				padding: 0;
				position: relative;
			}

		body::before {
			content: "";
			position: fixed;
			inset: -20px;
			z-index: -2;
			background-image: url("/optimized/anim_bg_3.webp");
			background-size: cover;
			background-position: center;
			background-repeat: no-repeat;
			filter: blur(5px) brightness(0.95) saturate(1.25);
			-webkit-filter: blur(5px) brightness(0.95) saturate(1.25);
			background-color: rgba(255, 255, 255, 0.05);
			background-blend-mode: overlay;
		}

			a {
				color: var(--bh-accent);
				text-decoration: none;
			}
			a:hover {
				text-decoration: underline;
			}

			.page-wrap {
				animation: bh-enter 0.5s cubic-bezier(0.1, 0.9, 0.2, 1) both;
			}
			@keyframes bh-enter {
				from {
					opacity: 0;
					transform: translateY(20px);
				}
				to {
					opacity: 1;
					transform: translateY(0);
				}
			}
			body.is-exiting .page-wrap {
				animation: bh-exit 0.28s cubic-bezier(0.4, 0, 1, 1) both;
				pointer-events: none;
			}
			@keyframes bh-exit {
				from {
					opacity: 1;
					transform: translateY(0);
				}
				to {
					opacity: 0;
					transform: translateY(20px);
				}
			}

			/* ===========================
			   LAYOUT CONTAINER
			=========================== */
			.bh-page {
				position: relative;
				z-index: 1;
				max-width: 1200px;
				margin: 0 auto;
				height: 100vh;
				padding: 16px 20px;
				box-sizing: border-box;
				display: flex;
				flex-direction: column;
				justify-content: center;
				align-items: center;
			}

			.bh-main-card {
				position: relative;
				background: transparent;
				border: none;
				box-shadow: none;
				backdrop-filter: none;
				-webkit-backdrop-filter: none;
				display: flex;
				height: calc(100vh - 32px);
				width: 100%;
				align-items: center;
				justify-content: center;
				box-sizing: border-box;
				overflow: visible;
				padding: 0;
			}

			/* ===========================
			   100% PERFECT CIRCULAR BUBBLE (THINK DIFFERENT)
			=========================== */
			.aero-component-bubble {
				position: relative;
				border-radius: 50% !important;
				background:
					/* Primary specular light spot - Top Left */
					radial-gradient(
						circle at 35% 26%,
						rgba(255, 255, 255, 0.8) 0%,
						rgba(224, 242, 254, 0.42) 28%,
						rgba(56, 189, 248, 0.18) 55%,
						rgba(14, 165, 233, 0.08) 80%,
						transparent 100%
					),
					/* Thin-film color tone */
					linear-gradient(
						135deg,
						rgba(255, 255, 255, 0.55) 0%,
						rgba(255, 255, 255, 0.22) 40%,
						rgba(255, 255, 255, 0.15) 60%,
						rgba(255, 255, 255, 0.4) 100%
					);
				backdrop-filter: blur(24px) saturate(170%);
				-webkit-backdrop-filter: blur(24px) saturate(170%);
				border: 1.5px solid rgba(255, 255, 255, 0.95);
				box-shadow:
					0 -0.0625em 0 0.0625em hsl(var(--hue), 90%, 92%) inset,
					0 0 0 0.0625em hsl(var(--hue), 90%, 75%) inset,
					0 0 0.35em 0.35em hsla(var(--hue), 90%, 70%, 0.45) inset,
					inset -8px -10px 28px rgba(2, 132, 199, 0.35),
					0 18px 50px rgba(14, 165, 233, 0.3),
					0 4px 16px rgba(0, 0, 0, 0.08);
				overflow: hidden;
				transition: transform 0.35s cubic-bezier(0.175, 0.885, 0.32, 1.275), box-shadow 0.3s ease;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				text-align: center;
				box-sizing: border-box;
				flex-shrink: 0;
			}

			.aero-component-bubble::before {
				content: "";
				position: absolute;
				top: 50%;
				left: 50%;
				width: 88%;
				height: 88%;
				border-radius: 50%;
				background-image: radial-gradient(25% 10% at 50% 5%, #ffffff 48%, rgba(255, 255, 255, 0) 50%);
				transform: translate(-50%, -50%) rotate(-45deg);
				pointer-events: none;
				z-index: 2;
			}

			.aero-component-bubble::after {
				content: "";
				position: absolute;
				bottom: 6%;
				right: 12%;
				width: 30%;
				height: 20%;
				border-radius: 50%;
				background: radial-gradient(
					circle,
					rgba(186, 230, 253, 0.75) 0%,
					rgba(125, 211, 252, 0.25) 70%,
					transparent 100%
				);
				filter: blur(3.5px);
				pointer-events: none;
				z-index: 2;
			}

			/* Staggered Floating Animations for Bubble Cluster */
			.aero-float-1 { animation: bh-component-float-1 6.5s ease-in-out 0s infinite alternate; }
			.aero-float-2 { animation: bh-component-float-2 7.2s ease-in-out 0.9s infinite alternate; }
			.aero-float-3 { animation: bh-component-float-3 6.8s ease-in-out 1.8s infinite alternate; }
			.aero-float-4 { animation: bh-component-float-4 7.6s ease-in-out 2.5s infinite alternate; }

			@keyframes bh-component-float-1 {
				0% { transform: translateY(0) scale(1); }
				50% { transform: translateY(-7px) scale(1.01); }
				100% { transform: translateY(0) scale(1); }
			}
			@keyframes bh-component-float-2 {
				0% { transform: translateY(0) scale(1); }
				50% { transform: translateY(-8px) scale(1.01); }
				100% { transform: translateY(0) scale(1); }
			}
			@keyframes bh-component-float-3 {
				0% { transform: translateY(0) scale(1); }
				50% { transform: translateY(-6px) scale(1.01); }
				100% { transform: translateY(0) scale(1); }
			}
			@keyframes bh-component-float-4 {
				0% { transform: translateY(0) scale(1); }
				50% { transform: translateY(-9px) scale(1.01); }
				100% { transform: translateY(0) scale(1); }
			}

			.hidden {
				display: none !important;
			}

			/* ===========================
			   PRECISION BUBBLE CONSTELLATION (0% COLLISION)
			=========================== */
			.wbc-bubble-stage {
				position: relative;
				width: 100%;
				max-width: 1100px;
				height: 560px;
				margin: -85px auto 0 auto;
			}

			/* CENTER MICROSCOPE ORB — 380px */
			.wbc-center-orb {
				position: absolute;
				left: calc(50% - 190px);
				top: calc(50% - 190px);
				width: 380px;
				height: 380px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				z-index: 5;
				overflow: hidden;
				isolation: isolate;
			}

			.wbc-center-content {
				width: 100%;
				height: 100%;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				position: absolute;
				inset: 0;
				border-radius: 50%;
				overflow: hidden;
			}

			.wbc-empty-state {
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				flex: 1;
				text-align: center;
				padding: 48px 24px;
				color: var(--bh-muted);
			}
			.wbc-empty-icon {
				width: 80px;
				height: 80px;
				border-radius: 50%;
				background: rgba(0, 0, 0, 0.02);
				border: 1px solid var(--bh-border);
				display: flex;
				align-items: center;
				justify-content: center;
				color: var(--bh-accent);
				margin-bottom: 24px;
				animation: wbc-pulse 3s infinite ease-in-out;
			}
			@keyframes wbc-pulse {
				0%,
				100% {
					transform: scale(1);
					opacity: 0.7;
				}
				50% {
					transform: scale(1.05);
					opacity: 1;
				}
			}
			.wbc-empty-title {
				font-size: 1.4rem;
				font-weight: 300;
				color: var(--bh-text);
				margin: 0 0 12px 0;
			}
			.wbc-empty-subtitle {
				font-size: 0.9rem;
				max-width: 420px;
				margin: 0;
				line-height: 1.6;
			}

			@media (max-width: 900px) {
				.bh-page {
					height: auto;
					padding: 16px;
				}
				.bh-main-card {
					flex-direction: column;
					padding: 24px;
					gap: 24px;
					height: auto;
					overflow: visible;
					backdrop-filter: blur(16px) saturate(130%);
					-webkit-backdrop-filter: blur(16px) saturate(130%);
				}
				.wbc-sidebar {
					width: 100% !important;
					border-right: none !important;
					padding-right: 0 !important;
					padding-bottom: 24px;
					border-bottom: 1px solid var(--bh-border);
					overflow: visible;
				}
				.wbc-workspace {
					overflow: visible;
					height: auto;
					padding-right: 0;
				}
				/* Reduce GPU-heavy backdrop-filter on mobile */
				.wbc-upload-zone,
				.wbc-preview-container,
				.wbc-image-panel,
				.wbc-count-card,
				.wbc-chart-card,
				.wbc-crop-card,
				.wbc-guide-note {
					backdrop-filter: none;
					-webkit-backdrop-filter: none;
					background: rgba(255, 255, 255, 0.35);
				}
			}

			/* ===========================
			   TOP NAV BAR (legacy / fallback)
			=========================== */
			.bh-topbar {
				display: none;
			}

			.wbc-back-btn {
				width: 40px;
				height: 40px;
				background: linear-gradient(
					180deg,
					rgba(186, 230, 253, 0.9) 0%,
					rgba(125, 211, 252, 0.7) 48%,
					rgba(56, 189, 248, 0.8) 50%,
					rgba(14, 165, 233, 0.9) 100%
				);
				color: #ffffff;
				display: flex;
				align-items: center;
				justify-content: center;
				border: 1px solid rgba(3, 105, 161, 0.5);
				border-top: 1.5px solid rgba(186, 230, 253, 0.9);
				border-radius: 50%;
				box-shadow:
					inset 0 2px 0 rgba(255, 255, 255, 0.7),
					inset 0 -1px 0 rgba(0, 0, 0, 0.1),
					0 4px 16px rgba(14, 165, 233, 0.25);
				cursor: pointer;
				transition: all 0.25s cubic-bezier(0.1, 0.8, 0.2, 1);
				flex-shrink: 0;
			}
			.wbc-back-btn:hover {
				background: linear-gradient(
					180deg,
					rgba(224, 242, 254, 0.95) 0%,
					rgba(186, 230, 253, 0.8) 48%,
					rgba(125, 211, 252, 0.85) 50%,
					rgba(56, 189, 248, 0.95) 100%
				);
				box-shadow:
					inset 0 2px 0 rgba(255, 255, 255, 0.9),
					0 6px 20px rgba(14, 165, 233, 0.35);
				transform: translateY(-1px) scale(1.04);
			}

			.bh-content-scroll {
				font-family: "Google Sans Flex", "Google Sans", sans-serif;
			}

			/* ===========================
			   HEADER & TITLE
			=========================== */
			.bh-header {
				position: absolute;
				left: 11%;
				top: 15%;
				width: 165px;
				height: 165px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				text-align: center;
				padding: 16px;
				box-sizing: border-box;
				z-index: 6;
			}

			.bh-title {
				font-size: 1.6rem;
				font-weight: 900;
				line-height: 1.1;
				letter-spacing: -0.5px;
				margin: 0;
				display: flex;
				flex-direction: row;
				align-items: center;
				justify-content: center;
				gap: 8px;
			}

			.title-nextwbc {
				font-family: "Google Sans Flex", "Google Sans", sans-serif;
				color: var(--bh-text);
			}
			.title-classifier {
				font-family: "Google Sans Flex", "Google Sans", sans-serif;
				color: var(--bh-accent);
			}

			.bh-subtitle {
				font-family: "Google Sans Flex", "Google Sans", sans-serif;
				font-size: 0.65rem;
				color: var(--bh-muted);
				font-weight: 400;
				margin: 0;
				line-height: 1.2;
				max-width: 140px;
				text-align: center;
			}

			/* ===========================
			   WBC DEMO COMPONENTS
			=========================== */
			#wbcUploadPrompt {
				width: 100%;
				height: 100%;
				display: flex;
				align-items: center;
				justify-content: center;
				position: absolute;
				inset: 0;
			}
			.wbc-upload-zone {
				position: absolute;
				inset: 0;
				width: 100%;
				height: 100%;
				border-radius: 50% !important;
				border: none;
				cursor: pointer;
				padding: 0;
				margin: 0;
				display: flex;
				align-items: center;
				justify-content: center;
				box-sizing: border-box;
			}

			/* ── User Guide Styling ── */
			.wbc-guide {
				position: absolute;
				left: 5%;
				top: 48%;
				width: 195px;
				height: 195px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				text-align: center;
				padding: 18px 16px;
				box-sizing: border-box;
				z-index: 6;
			}
			.wbc-guide-title {
				font-size: 0.75rem;
				font-weight: 700;
				text-transform: uppercase;
				letter-spacing: 1px;
				color: var(--bh-text);
				margin: 0;
				border-bottom: none;
				padding-bottom: 0;
			}
			.wbc-guide-list {
				list-style: none;
				padding: 0;
				margin: 0;
				display: flex;
				flex-direction: column;
				gap: 4px;
				align-items: center;
			}
			.wbc-guide-list li {
				display: flex;
				gap: 6px;
				align-items: center;
				text-align: left;
				max-width: 170px;
			}
			.wbc-step-num {
				width: 16px;
				height: 16px;
				border-radius: 50%;
				background: var(--bh-accent);
				color: #ffffff;
				font-size: 0.6rem;
				font-weight: bold;
				display: flex;
				align-items: center;
				justify-content: center;
				flex-shrink: 0;
			}
			.wbc-step-text {
				font-size: 0.62rem;
				color: var(--bh-muted);
				line-height: 1.2;
			}
			.wbc-guide-note {
				font-size: 0.6rem;
				background: rgba(255, 255, 255, 0.45);
				border: 1px solid rgba(255, 255, 255, 0.7);
				border-radius: 99px;
				padding: 3px 8px;
				color: var(--bh-muted);
				line-height: 1.2;
				box-shadow: 0 2px 6px rgba(0, 0, 0, 0.04);
			}
			.wbc-upload-icon-wrapper {
				width: 88px;
				height: 88px;
				border-radius: 50%;
				background: rgba(255, 255, 255, 0.65);
				border: 2px solid rgba(255, 255, 255, 0.95);
				box-shadow:
					0 8px 28px rgba(14, 165, 233, 0.3),
					inset 0 2px 5px rgba(255, 255, 255, 1);
				display: flex;
				align-items: center;
				justify-content: center;
				margin: 0;
				padding: 0;
				color: var(--bh-accent);
				transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
			}
			.wbc-upload-icon-wrapper svg {
				display: block;
				margin: auto;
			}
			.wbc-upload-zone:hover .wbc-upload-icon-wrapper {
				transform: scale(1.15);
				color: #0284c7;
				box-shadow:
					0 12px 36px rgba(14, 165, 233, 0.45),
					inset 0 2px 6px rgba(255, 255, 255, 1);
			}
			.wbc-upload-text {
				font-size: 0.82rem;
				font-weight: 500;
				color: rgba(0, 0, 0, 0.75);
				margin: 0 0 4px 0;
			}
			.wbc-upload-subtext {
				font-size: 0.65rem;
				text-transform: uppercase;
				letter-spacing: 1px;
				color: rgba(0, 0, 0, 0.45);
				margin: 0;
			}

			.wbc-preview-container {
				width: 380px;
				height: 380px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				text-align: center;
				padding: 24px;
				box-sizing: border-box;
			}
			.wbc-preview-img {
				width: 115px;
				height: 115px;
				border-radius: 50%;
				object-fit: cover;
				border: 2.5px solid rgba(255, 255, 255, 0.9);
				box-shadow: 0 4px 14px rgba(0, 0, 0, 0.12);
			}

			.wbc-progress-bar-wrapper {
				width: 100%;
				max-width: 220px;
				height: 5px;
				background: rgba(0, 0, 0, 0.08);
				border-radius: 99px;
				overflow: hidden;
				margin-top: 10px;
			}
			.wbc-progress-bar-fill {
				height: 100%;
				width: 0%;
				background: var(--bh-accent);
				border-radius: 99px;
				transition: width 0.4s ease-out;
			}
			.wbc-status-text {
				font-size: 0.75rem;
				color: var(--bh-accent);
				display: flex;
				align-items: center;
				gap: 8px;
				justify-content: center;
				margin-top: 8px;
			}

			.wbc-error-msg {
				padding: 12px;
				border-radius: 12px;
				background: rgba(239, 68, 68, 0.08);
				border: 1px solid rgba(239, 68, 68, 0.15);
				color: var(--bh-danger);
				font-size: 0.8rem;
				margin-top: 12px;
				text-align: center;
			}

			/* Results structure */
			.wbc-results-container {
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				width: 100%;
				height: 100%;
			}
			.wbc-results-header {
				display: flex;
				align-items: center;
				justify-content: space-between;
				flex-wrap: wrap;
				border-bottom: 1px solid var(--bh-border);
				padding-bottom: 12px;
			}
			.wbc-results-heading {
				font-size: 1.1rem;
				font-weight: 300;
				margin: 0;
			}
			.wbc-results-top {
				display: flex;
				flex-direction: row;
				gap: 24px;
				flex: 1;
				align-items: stretch;
				min-height: 0;
			}
			@media (max-width: 560px) {
				.wbc-results-top {
					flex-direction: column;
				}
				.wbc-image-panel {
					height: 280px;
					flex: none;
				}
			}
			.wbc-image-panel {
				width: 380px;
				height: 380px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				overflow: hidden;
				position: relative;
				flex-shrink: 0;
			}
			.wbc-image-wrapper {
				position: absolute;
				inset: 0;
				display: flex;
				align-items: center;
				justify-content: center;
				overflow: hidden;
				border-radius: 50%;
			}
			.wbc-image-wrapper img {
				width: 100%;
				height: 100%;
				object-fit: cover;
				cursor: pointer;
				border-radius: 50%;
			}
			.wbc-panel-badge {
				position: absolute;
				top: 24px;
				left: 50%;
				transform: translateX(-50%);
				z-index: 2;
				font-size: 0.65rem;
				font-weight: bold;
				background: rgba(255, 255, 255, 0.85);
				color: var(--bh-accent);
				padding: 4px 12px;
				border-radius: 99px;
				letter-spacing: 1px;
				text-transform: uppercase;
				border: 1px solid rgba(255, 255, 255, 0.9);
				box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
			}

			/* LEFT CLUSTER ORBS */
			.wbc-back-bubble {
				position: absolute;
				left: calc(50% - 425px);
				top: calc(50% - 210px);
				width: 42px;
				height: 42px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				z-index: 7;
				display: flex;
				align-items: center;
				justify-content: center;
				color: var(--bh-accent);
				cursor: pointer;
				text-decoration: none;
				transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
			}
			.wbc-back-bubble:hover {
				transform: scale(1.15) translateY(-2px);
				color: #0284c7;
			}

			.bh-header {
				position: absolute;
				left: calc(50% - 370px);
				top: calc(50% - 170px);
				width: 170px;
				height: 170px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				text-align: center;
				padding: 16px;
				box-sizing: border-box;
				z-index: 6;
			}

			.wbc-guide {
				position: absolute;
				left: calc(50% - 380px);
				top: calc(50% + 15px);
				width: 195px;
				height: 195px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				text-align: center;
				padding: 18px 16px;
				box-sizing: border-box;
				z-index: 6;
			}

			.wbc-sample-section {
				position: absolute;
				left: calc(50% - 215px);
				top: calc(50% + 155px);
				width: 140px;
				height: 140px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				overflow: hidden;
				padding: 0;
				margin: 0;
				box-sizing: border-box;
				z-index: 6;
				cursor: pointer;
			}

			/* RIGHT CLUSTER ORBS (Symmetrical counterparts) */
			.wbc-count-card {
				position: absolute;
				left: calc(50% + 210px);
				top: calc(50% - 170px);
				width: 130px;
				height: 130px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				text-align: center;
				z-index: 6;
			}

			.wbc-chart-card {
				position: absolute;
				left: calc(50% + 195px);
				top: calc(50% + 15px);
				width: 195px;
				height: 195px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				text-align: center;
				padding: 10px 14px;
				box-sizing: border-box;
				z-index: 6;
			}

			.wbc-chart-title {
				font-size: 0.64rem;
				font-weight: 700;
				text-transform: uppercase;
				letter-spacing: 1px;
				color: var(--bh-text);
				margin: 0 0 6px 0;
			}

			#wbcDistributionContainer {
				width: 100%;
				max-width: 155px;
				display: flex;
				flex-direction: column;
				gap: 3px;
			}

			.wbc-count-bar-item {
				display: flex;
				align-items: center;
				gap: 5px;
				width: 100%;
				font-family: "Google Sans Flex", "Google Sans", sans-serif;
			}

			.wbc-bar-dot {
				width: 6px;
				height: 6px;
				border-radius: 50%;
				flex-shrink: 0;
			}

			.wbc-bar-label {
				width: 58px;
				font-size: 0.58rem;
				font-weight: 600;
				color: var(--bh-text);
				text-align: left;
				white-space: nowrap;
				overflow: hidden;
				text-overflow: ellipsis;
				opacity: 0.88;
			}

			.wbc-bar-track {
				flex: 1;
				height: 4px;
				background: rgba(0, 0, 0, 0.08);
				border-radius: 99px;
				overflow: hidden;
			}

			.wbc-bar-fill {
				height: 100%;
				border-radius: 99px;
				transition: width 0.6s cubic-bezier(0.1, 0.8, 0.2, 1);
			}

			.wbc-bar-val {
				width: 12px;
				text-align: right;
				font-size: 0.58rem;
				font-weight: 700;
				font-family: monospace;
			}

			.wbc-reset-orb {
				position: absolute;
				left: calc(50% + 125px);
				top: calc(50% + 145px);
				width: 80px;
				height: 80px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				display: flex;
				align-items: center;
				justify-content: center;
				text-align: center;
				padding: 4px;
				box-sizing: border-box;
				z-index: 6;
			}

			.wbc-reset-btn {
				display: flex;
				align-items: center;
				justify-content: center;
				width: 100%;
				height: 100%;
				padding: 0;
				background: transparent;
				border: none;
				color: var(--bh-accent);
				cursor: pointer;
				box-shadow: none;
				transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
			}
			.wbc-reset-btn:hover {
				transform: scale(1.2) rotate(45deg);
				color: #0284c7;
			}

			/* SATELLITE CELL CROPS ORBITING POSITIONS */
			.wbc-crops-cluster-field {
				position: absolute;
				inset: 0;
				pointer-events: none;
				z-index: 15;
			}

			.wbc-crop-card,
			.wbc-crop-bubble {
				width: 84px;
				height: 84px;
				aspect-ratio: 1 / 1;
				border-radius: 50% !important;
				position: absolute;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				padding: 6px;
				box-sizing: border-box;
				cursor: pointer;
				transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
				pointer-events: auto;
				animation: wbc-crop-float 4.5s ease-in-out infinite alternate;
			}

			@keyframes wbc-crop-float {
				0% { transform: scale(1) translateY(0px) rotate(0deg); }
				50% { transform: scale(1.06) translateY(-6px) rotate(3deg); }
				100% { transform: scale(0.97) translateY(4px) rotate(-2deg); }
			}

			.wbc-crop-bubble:hover,
			.wbc-crop-card:hover {
				transform: scale(1.25) translateY(-6px) !important;
				z-index: 30;
			}

			/* Cropped cell bubbles popping animation */
			.wbc-crop-bubble.is-popping {
				animation: wbc-bubble-pop 0.45s cubic-bezier(0.1, 0.9, 0.2, 1.2) forwards !important;
			}
			@keyframes wbc-bubble-pop {
				0% {
					transform: scale(1);
					opacity: 1;
					filter: blur(0px);
				}
				35% {
					transform: scale(1.4);
					opacity: 0.95;
					filter: blur(2px) brightness(1.4);
				}
				100% {
					transform: scale(1.85);
					opacity: 0;
					filter: blur(14px) brightness(2.5);
				}
			}

			/* Central magic mirror dissolve animation */
			.wbc-results-container.is-dissolving {
				animation: wbc-magic-dissolve 0.5s cubic-bezier(0.4, 0, 0.2, 1) forwards;
			}
			@keyframes wbc-magic-dissolve {
				0% {
					opacity: 1;
					filter: blur(0px) brightness(1);
					transform: scale(1);
				}
				50% {
					opacity: 0.6;
					filter: blur(16px) brightness(1.7) contrast(1.2);
					transform: scale(1.04);
				}
				100% {
					opacity: 0;
					filter: blur(28px) brightness(2.2) contrast(0.7);
					transform: scale(0.95);
				}
			}

			/* Clustered Satellite Cell Crops in Top-Right Zone (Where Cells Detected was) */
			.wbc-crop-orbit-tr {
				left: calc(50% + 215px);
				top: calc(50% - 195px);
				animation-delay: 0s;
			}
			.wbc-crop-orbit-bl {
				left: calc(50% + 305px);
				top: calc(50% - 140px);
				animation-delay: 1.1s;
			}
			.wbc-crop-orbit-br {
				left: calc(50% + 205px);
				top: calc(50% - 90px);
				animation-delay: 2.2s;
			}
			.wbc-crop-orbit-tl {
				left: calc(50% + 310px);
				top: calc(50% - 225px);
				animation-delay: 0.7s;
			}
			.wbc-crop-orbit-mr {
				left: calc(50% + 120px);
				top: calc(50% - 210px);
				animation-delay: 1.6s;
			}
			.wbc-crop-orbit-ml {
				left: calc(50% + 380px);
				top: calc(50% - 150px);
				animation-delay: 2.8s;
			}

			.wbc-crop-img-wrap {
				width: 56px;
				height: 56px;
				border-radius: 50%;
				overflow: hidden;
				display: flex;
				align-items: center;
				justify-content: center;
				border: 1.5px solid rgba(255, 255, 255, 0.95);
				box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
				padding: 0;
			}

			.wbc-crop-img-wrap img {
				width: 100%;
				height: 100%;
				object-fit: cover;
				border-radius: 50%;
			}

			.wbc-crop-info {
				position: absolute;
				bottom: -4px;
				font-size: 0.58rem;
				font-weight: 700;
				padding: 2px 7px;
				border-radius: 99px;
				display: flex;
				gap: 4px;
				align-items: center;
				box-shadow: 0 3px 8px rgba(0, 0, 0, 0.25);
				border: 1.5px solid rgba(255, 255, 255, 0.9);
				white-space: nowrap;
				letter-spacing: 0.3px;
			}

			.wbc-crop-label {
				text-transform: capitalize;
			}
			.wbc-crop-conf {
				opacity: 0.9;
				font-size: 0.55rem;
			}

			/* Quick Try Full Glass Photo Bubble */
			.wbc-sample-btn {
				position: relative;
				width: 100%;
				height: 100%;
				display: flex;
				align-items: center;
				justify-content: center;
				padding: 0;
				margin: 0;
				background: transparent;
				border: none;
				border-radius: 50%;
				overflow: hidden;
				cursor: pointer;
				transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
			}

			.wbc-sample-btn:hover:not(:disabled) {
				transform: scale(1.08);
			}

			.wbc-sample-btn:disabled {
				opacity: 0.6;
				cursor: not-allowed;
			}

			.wbc-sample-bg-img {
				position: absolute;
				inset: 0;
				width: 100%;
				height: 100%;
				object-fit: cover;
				border-radius: 50%;
				transition: transform 0.4s ease;
			}

			.wbc-sample-btn:hover:not(:disabled) .wbc-sample-bg-img {
				transform: scale(1.12);
			}

			.wbc-sample-overlay {
				position: absolute;
				inset: 0;
				border-radius: 50%;
				background: radial-gradient(
					circle at 50% 50%,
					rgba(0, 0, 0, 0.15) 0%,
					rgba(0, 0, 0, 0.5) 75%,
					rgba(2, 132, 199, 0.65) 100%
				);
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				gap: 2px;
				padding: 10px;
				box-sizing: border-box;
				text-align: center;
				backdrop-filter: blur(1px);
				-webkit-backdrop-filter: blur(1px);
				box-shadow:
					inset 0 2px 6px rgba(255, 255, 255, 0.7),
					inset 0 -3px 8px rgba(0, 0, 0, 0.4);
				transition: background 0.3s ease;
			}

			.wbc-sample-btn:hover:not(:disabled) .wbc-sample-overlay {
				background: radial-gradient(
					circle at 50% 50%,
					rgba(0, 0, 0, 0.08) 0%,
					rgba(0, 0, 0, 0.4) 75%,
					rgba(2, 132, 199, 0.75) 100%
				);
			}

			.wbc-sample-tag {
				font-size: 0.62rem;
				font-weight: 800;
				letter-spacing: 1.2px;
				text-transform: uppercase;
				color: #ffffff;
				text-shadow:
					0 1px 4px rgba(0, 0, 0, 0.8),
					0 0 10px rgba(56, 189, 248, 0.8);
				background: rgba(2, 132, 199, 0.65);
				border: 1px solid rgba(255, 255, 255, 0.8);
				border-radius: 99px;
				padding: 2px 8px;
				margin-bottom: 2px;
			}

			.wbc-sample-name {
				font-size: 0.62rem;
				font-weight: 700;
				color: #ffffff;
				text-shadow: 0 1px 4px rgba(0, 0, 0, 0.9);
				white-space: nowrap;
				overflow: hidden;
				text-overflow: ellipsis;
				max-width: 105px;
			}

			.wbc-sample-desc {
				font-size: 0.54rem;
				color: rgba(255, 255, 255, 0.85);
				text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
			}

			.wbc-toggle-btn {
				display: block;
				margin: 16px auto 0;
				background: none;
				border: none;
				color: var(--bh-muted);
				font-size: 0.75rem;
				text-transform: uppercase;
				letter-spacing: 1px;
				cursor: pointer;
				padding: 6px 12px;
				transition: color 0.2s;
			}
			.wbc-toggle-btn:hover {
				color: var(--bh-text);
			}

			.wbc-reset-btn {
				background: linear-gradient(
					180deg,
					rgba(255, 255, 255, 0.6) 0%,
					rgba(255, 255, 255, 0.2) 48%,
					rgba(255, 255, 255, 0.08) 50%,
					rgba(255, 255, 255, 0.3) 100%
				);
				backdrop-filter: blur(12px) saturate(140%);
				-webkit-backdrop-filter: blur(12px) saturate(140%);
				border: 1px solid rgba(255, 255, 255, 0.6);
				border-top: 1.5px solid rgba(255, 255, 255, 0.85);
				color: var(--bh-text);
				cursor: pointer;
				padding: 6px 14px;
				border-radius: 99px;
				display: inline-flex;
				align-items: center;
				justify-content: center;
				gap: 8px;
				box-shadow:
					0 3px 10px rgba(0, 0, 0, 0.06),
					inset 0 2px 0 rgba(255, 255, 255, 0.7),
					inset 0 -1px 0 rgba(0, 0, 0, 0.04);
				transition: all 0.25s cubic-bezier(0.1, 0.8, 0.2, 1);
			}
			.wbc-reset-btn:hover {
				background: linear-gradient(
					135deg,
					rgba(255, 200, 200, 0.3) 0%,
					rgba(239, 68, 68, 0.08) 100%
				);
				color: #ef4444;
				border-color: rgba(239, 68, 68, 0.35);
				box-shadow:
					0 4px 12px rgba(239, 68, 68, 0.08),
					inset 0 1px 0 rgba(255, 255, 255, 0.5);
			}
			.wbc-reset-btn span {
				font-size: 0.8rem;
				font-weight: bold;
				letter-spacing: 0.5px;
			}

			/* ===========================
			   TOP-RIGHT RGB BUBBLE TOGGLE BUTTON
			=========================== */
			.rgb-bubble-toggle-btn {
				position: absolute;
				top: 24px;
				right: 28px;
				width: 46px;
				height: 46px;
				border-radius: 50%;
				border: none;
				background: transparent;
				cursor: pointer;
				z-index: 50;
				padding: 0;
				outline: none;
				transition: transform 0.25s cubic-bezier(0.1, 0.9, 0.2, 1);
			}

			.rgb-bubble-toggle-btn:hover {
				transform: scale(1.14);
			}

			.rgb-bubble-toggle-btn:active {
				transform: scale(0.92);
			}

			/* Vibrant RGB Glass Bubble (ON State) */
			.rgb-bubble-inner {
				position: absolute;
				inset: 0;
				border-radius: 50%;
				background:
					radial-gradient(
						circle at 35% 26%,
						rgba(255, 255, 255, 1) 0%,
						rgba(255, 255, 255, 0.75) 15%,
						rgba(192, 132, 252, 0.45) 40%,
						rgba(56, 189, 248, 0.4) 65%,
						rgba(34, 197, 94, 0.35) 85%,
						transparent 100%
					),
					linear-gradient(
						135deg,
						#38bdf8 0%,
						#a855f7 50%,
						#22c55e 100%
					);
				background-blend-mode: overlay;
				border: 1.8px solid rgba(255, 255, 255, 0.95);
				box-shadow:
					inset -4px -5px 12px rgba(168, 85, 247, 0.5),
					inset 4px 5px 10px rgba(255, 255, 255, 0.98),
					0 0 16px rgba(168, 85, 247, 0.5),
					0 0 28px rgba(56, 189, 248, 0.4);
				animation: rgb-hue-rotate 5s infinite linear;
				transition:
					transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.25),
					opacity 0.25s ease;
			}

			/* Top Crescent Specular Glare */
			.rgb-bubble-inner::before {
				content: "";
				position: absolute;
				top: 10%;
				left: 18%;
				width: 38%;
				height: 26%;
				border-radius: 50%;
				background: linear-gradient(
					180deg,
					#ffffff 0%,
					rgba(255, 255, 255, 0.25) 100%
				);
				transform: rotate(-30deg);
			}

			@keyframes rgb-hue-rotate {
				0% {
					filter: hue-rotate(0deg);
				}
				100% {
					filter: hue-rotate(360deg);
				}
			}

			/* Ghost Outline (OFF State - Faint & Blurry) */
			.ghost-bubble-outline {
				position: absolute;
				inset: 0;
				border-radius: 50%;
				border: 1.5px dashed rgba(255, 255, 255, 0.55);
				background: rgba(255, 255, 255, 0.12);
				backdrop-filter: blur(4px);
				-webkit-backdrop-filter: blur(4px);
				box-shadow: inset 0 1px 3px rgba(255, 255, 255, 0.5);
				opacity: 0;
				transform: scale(0.85);
				transition:
					opacity 0.3s ease,
					transform 0.3s ease,
					border-color 0.2s ease;
				pointer-events: none;
			}

			/* OFF State Behavior */
			.rgb-bubble-toggle-btn.is-off .rgb-bubble-inner {
				opacity: 0;
				transform: scale(0);
				pointer-events: none;
			}

			.rgb-bubble-toggle-btn.is-off .ghost-bubble-outline {
				opacity: 0.35;
				transform: scale(1);
			}

			.rgb-bubble-toggle-btn.is-off:hover .ghost-bubble-outline {
				opacity: 0.75;
				border-color: rgba(255, 255, 255, 0.95);
				background: rgba(255, 255, 255, 0.25);
			}

			/* ===========================
			   ULTRA-REALISTIC AERO GLASS BUBBLE
			=========================== */
			.aero-bubble-stage {
				position: fixed;
				inset: 0;
				pointer-events: none;
				overflow: hidden;
				transition: opacity 0.4s ease;
			}
			.aero-stage-back {
				z-index: -1; /* 80% bubbles float behind main UI */
			}
			.aero-stage-front {
				z-index: 5; /* 20% bubbles float in front of main UI */
			}

			.aero-glass-bubble {
				position: absolute;
				top: 0;
				left: 0;
				border-radius: 50%;
				box-sizing: border-box;
				box-shadow:
					0 -0.0625em 0 0.0625em hsl(var(--hue), 90%, 92%) inset,
					0 0 0 0.0625em hsl(var(--hue), 90%, 75%) inset,
					0 0 0.28em 0.28em hsla(var(--hue), 90%, 70%, 0.65) inset,
					0 10px 28px rgba(14, 165, 233, 0.35);
				background: radial-gradient(
					circle at 35% 26%,
					rgba(255, 255, 255, 0.4) 0%,
					rgba(224, 242, 254, 0.15) 30%,
					rgba(56, 189, 248, 0.1) 70%,
					transparent 100%
				);
				backdrop-filter: blur(2.5px) saturate(160%);
				-webkit-backdrop-filter: blur(2.5px) saturate(160%);
				pointer-events: auto;
				cursor: pointer;
				will-change: transform, opacity;
			}

			.aero-glass-bubble::after {
				content: "";
				position: absolute;
				top: 50%;
				left: 50%;
				width: 87.5%;
				height: 87.5%;
				border-radius: 50%;
				background-image: radial-gradient(
					25% 10% at 50% 5%,
					hsl(0, 0%, 100%) 48%,
					hsla(0, 0%, 100%, 0) 50%
				);
				transform: translate(-50%, -50%) rotate(-45deg);
				pointer-events: none;
			}

			.aero-glass-bubble.is-popping {
				transform: scale(1.35) !important;
				opacity: 0 !important;
				transition:
					transform 0.18s cubic-bezier(0.12, 0, 0.39, 0),
					opacity 0.18s ease-out !important;
			}

			/* ── 7-Drop Radial Pop Burst Particle ── */
			.aero-pop-burst {
				position: absolute;
				pointer-events: none;
				z-index: 20;
				width: 0;
				height: 0;
			}
			.aero-pop-drop {
				position: absolute;
				top: 0;
				left: 0;
				width: 2.5px;
				height: 10px;
				transform-origin: 50% 0;
				margin-left: -1.25px;
			}
			.aero-pop-drop::before {
				content: "";
				display: block;
				width: 100%;
				height: 100%;
				background-color: hsl(var(--hue), 90%, 80%);
				border-radius: 1px;
				animation: bubble-drop-pop 0.4s cubic-bezier(0.33, 1, 0.68, 1) forwards;
				transform-origin: 50% 0;
			}
			@keyframes bubble-drop-pop {
				0% {
					visibility: visible;
					transform: translateY(0) scaleY(1);
					opacity: 1;
				}
				65% {
					visibility: visible;
					transform: translateY(var(--drop-dist, 28px)) scaleY(1.2);
					opacity: 0.9;
				}
				100% {
					transform: translateY(calc(var(--drop-dist, 28px) * 1.5)) scaleY(0);
					opacity: 0;
				}
			}

			/* ===========================
			   CORAL REEF BOTTOM LAYER (FULL WIDTH 100VW)
			=========================== */
			.coralreef-bottom-layer {
				position: fixed;
				bottom: 0;
				left: 0;
				right: 0;
				width: 100vw;
				height: 112px;
				pointer-events: none;
				z-index: 100; /* Topmost layer */
				overflow: hidden;
				display: flex;
				align-items: flex-end;
				transition: opacity 0.4s ease;
			}

			.coralreef-img {
				width: 100vw;
				height: 112px;
				object-fit: cover;
				object-position: bottom center;
				display: block;
				filter: drop-shadow(0 -8px 22px rgba(14, 165, 233, 0.45))
					saturate(1.25);
				opacity: 0.95;
			}

			/* ── Decorative Micro Glass Bubbles ── */
			.wbc-deco-bubble {
				position: absolute;
				border-radius: 50% !important;
				pointer-events: none;
				z-index: 3;
				box-shadow:
					0 -0.0625em 0 0.0625em hsl(var(--hue), 90%, 92%) inset,
					0 0 0 0.0625em hsl(var(--hue), 90%, 75%) inset,
					0 0 0.25em 0.25em hsla(var(--hue), 90%, 70%, 0.7) inset,
					0 4px 12px rgba(14, 165, 233, 0.3);
				background: radial-gradient(
					circle at 35% 25%,
					rgba(255, 255, 255, 0.4) 0%,
					rgba(56, 189, 248, 0.15) 60%,
					transparent 100%
				);
				backdrop-filter: blur(2px);
				-webkit-backdrop-filter: blur(2px);
			}

			.wbc-deco-bubble::after {
				content: "";
				position: absolute;
				top: 50%;
				left: 50%;
				width: 87.5%;
				height: 87.5%;
				border-radius: 50%;
				background-image: radial-gradient(
					25% 10% at 50% 5%,
					hsl(0, 0%, 100%) 48%,
					hsla(0, 0%, 100%, 0) 50%
				);
				transform: translate(-50%, -50%) rotate(-45deg);
			}

			/* ===========================
			   PURE CSS 3-BUBBLE INFERENCE LOADER
			=========================== */
			.pl {
				display: flex;
				justify-content: space-between;
				align-items: center;
				filter: drop-shadow(0 8px 16px rgba(14, 165, 233, 0.4));
				margin: 0 auto 12px auto;
				width: 8.5em;
				height: 4.8em;
				position: relative;
				font-size: 15px;
			}

			.pl__bubble {
				position: relative;
				width: 2.4em;
				height: 2.4em;
			}

			.pl__bubble-drop {
				position: absolute;
				top: 50%;
				left: 50%;
				width: 0.125em;
				height: 0.5em;
				transform: translate(-50%, -3em);
				transform-origin: 50% 0;
			}

			.pl__bubble-drop:before {
				content: "";
				display: block;
				position: absolute;
				width: 100%;
				height: 100%;
				animation: bubble-drop 1.5s linear infinite;
				background-color: hsl(var(--hue), 90%, 80%);
				border-radius: 0.0625em;
				transform: translateY(0) scaleY(0);
				transform-origin: 50% 0;
			}

			.pl__bubble-drop:nth-child(1) { transform: translate(-50%, -3em) rotate(0deg); }
			.pl__bubble-drop:nth-child(2) { transform: translate(-50%, -3em) rotate(51.428deg); }
			.pl__bubble-drop:nth-child(3) { transform: translate(-50%, -3em) rotate(102.857deg); }
			.pl__bubble-drop:nth-child(4) { transform: translate(-50%, -3em) rotate(154.285deg); }
			.pl__bubble-drop:nth-child(5) { transform: translate(-50%, -3em) rotate(205.714deg); }
			.pl__bubble-drop:nth-child(6) { transform: translate(-50%, -3em) rotate(257.142deg); }
			.pl__bubble-drop:nth-child(7) { transform: translate(-50%, -3em) rotate(308.571deg); }

			.pl__bubble:before,
			.pl__bubble:after {
				content: "";
				display: block;
				position: absolute;
				border-radius: 50%;
			}

			.pl__bubble:before {
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				animation: bubble-rise-before 1.5s linear infinite;
				box-shadow:
					0 -0.0625em 0 0.0625em hsl(var(--hue), 90%, 90%) inset,
					0 0 0 0.0625em hsl(var(--hue), 90%, 70%) inset,
					0 0 0.25em 0.25em hsla(var(--hue), 90%, 70%, 0.7) inset;
				transform: translate(0, 4.5em) scale(0);
			}

			.pl__bubble:after {
				top: 50%;
				left: 50%;
				width: 87.5%;
				height: 87.5%;
				animation: bubble-rise-after 1.5s linear infinite;
				background-image: radial-gradient(
					25% 10% at 50% 5%,
					hsl(0, 0%, 100%) 48%,
					hsla(0, 0%, 100%, 0) 50%
				);
				transform: translate(-50%, -50%) translate(0, 4.5em) rotate(-45deg) scale(0);
			}

			.pl__bubble:nth-child(2):before,
			.pl__bubble:nth-child(2):after,
			.pl__bubble:nth-child(2) .pl__bubble-drop:before {
				animation-delay: 0.15s;
			}

			.pl__bubble:nth-child(3):before,
			.pl__bubble:nth-child(3):after,
			.pl__bubble:nth-child(3) .pl__bubble-drop:before {
				animation-delay: 0.3s;
			}

			@keyframes bubble-rise-before {
				from {
					animation-timing-function: cubic-bezier(0.12, 0, 0.39, 0);
					transform: translate(0, 4.5em) scale(0);
				}
				30% {
					animation-timing-function: cubic-bezier(0.61, 1, 0.88, 1);
					transform: translate(0, 0.75em) scale(1);
				}
				60% {
					animation-timing-function: cubic-bezier(0.12, 0, 0.39, 0);
					opacity: 1;
					transform: translate(0, -3em) scale(1);
				}
				70%,
				to {
					opacity: 0;
					transform: translate(0, -3em) scale(0.25);
				}
			}

			@keyframes bubble-rise-after {
				from {
					animation-timing-function: cubic-bezier(0.12, 0, 0.39, 0);
					transform: translate(-50%, -50%) translate(0, 4.5em) rotate(-45deg) scale(0);
				}
				30% {
					animation-timing-function: cubic-bezier(0.61, 1, 0.88, 1);
					transform: translate(-50%, -50%) translate(0, 0.75em) rotate(-45deg) scale(1);
				}
				60% {
					animation-timing-function: cubic-bezier(0.12, 0, 0.39, 0);
					opacity: 1;
					transform: translate(-50%, -50%) translate(0, -3em) rotate(-45deg) scale(1);
				}
				70%,
				to {
					opacity: 0;
					transform: translate(-50%, -50%) translate(0, -3em) rotate(-45deg) scale(0.25);
				}
			}

			@keyframes bubble-drop {
				from {
					animation-timing-function: steps(1, end);
					visibility: hidden;
					transform: translateY(0) scaleY(1);
				}
				65% {
					animation-timing-function: cubic-bezier(0.33, 1, 0.68, 1);
					visibility: visible;
					transform: translateY(0) scaleY(1);
				}
				80%,
				to {
					transform: translateY(400%) scaleY(0);
				}
			}

			.wbc-deco-1 {
				width: 22px;
				height: 22px;
				left: calc(50% - 260px);
				top: calc(50% - 235px);
				animation: bh-component-float-1 6s ease-in-out infinite;
			}
			.wbc-deco-2 {
				width: 14px;
				height: 14px;
				left: calc(50% - 410px);
				top: calc(50% - 15px);
				animation: bh-component-float-2 4.8s ease-in-out infinite;
			}
			.wbc-deco-3 {
				width: 18px;
				height: 18px;
				left: calc(50% - 275px);
				top: calc(50% + 215px);
				animation: bh-component-float-3 5.5s ease-in-out infinite;
			}
			.wbc-deco-4 {
				width: 26px;
				height: 26px;
				left: calc(50% - 15px);
				top: calc(50% + 210px);
				animation: bh-component-float-4 7s ease-in-out infinite;
			}
			.wbc-deco-5 {
				width: 16px;
				height: 16px;
				left: calc(50% + 215px);
				top: calc(50% + 205px);
				animation: bh-component-float-1 5.2s ease-in-out infinite;
			}
			.wbc-deco-6 {
				width: 20px;
				height: 20px;
				left: calc(50% + 365px);
				top: calc(50% - 20px);
				animation: bh-component-float-2 6.4s ease-in-out infinite;
			}

			/* ── Swimming Tropical Fish Aquarium ── */
			.wbc-fish-aquarium-stage {
				position: fixed;
				inset: 0;
				width: 100vw;
				height: 100vh;
				pointer-events: none;
				overflow: hidden;
				z-index: 2;
			}

			.wbc-swimming-fish {
				position: absolute;
				pointer-events: auto;
				cursor: pointer;
				will-change: transform, left;
				transition: filter 0.3s ease, transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
			}

			.wbc-swimming-fish:hover,
			.wbc-swimming-fish.is-hovered {
				filter: drop-shadow(0 0 16px rgba(56, 189, 248, 0.95)) brightness(1.2) !important;
				transform: scale(1.1) translateY(-4px);
			}

			.wbc-swimming-fish:hover .wbc-fish-img,
			.wbc-swimming-fish.is-hovered .wbc-fish-img {
				animation-duration: 0.9s;
			}

			.fish-layer-back {
				opacity: 0.85;
				filter: drop-shadow(0 4px 10px rgba(0, 0, 0, 0.18)) brightness(0.95);
				z-index: 2;
			}

			.fish-layer-mid {
				opacity: 0.98;
				filter: drop-shadow(0 8px 20px rgba(2, 132, 199, 0.35)) brightness(1.05);
				z-index: 6;
			}

			.wbc-fish-img {
				width: 100%;
				height: auto;
				display: block;
				animation: fish-undulate 2.6s ease-in-out infinite alternate;
			}

			@keyframes fish-undulate {
				0% {
					transform: translateY(0px) rotate(0deg);
				}
				50% {
					transform: translateY(-8px) rotate(2.5deg);
				}
				100% {
					transform: translateY(6px) rotate(-2deg);
				}
			}

			@keyframes swim-ltr {
				0% {
					left: -180px;
				}
				100% {
					left: calc(100vw + 180px);
				}
			}

			@keyframes swim-rtl {
				0% {
					left: calc(100vw + 180px);
				}
				100% {
					left: -180px;
				}
			}

			.swim-left-to-right {
				animation-name: swim-ltr;
				animation-timing-function: linear;
				animation-iteration-count: 1;
				animation-fill-mode: forwards;
			}

			.swim-right-to-left {
				animation-name: swim-rtl;
				animation-timing-function: linear;
				animation-iteration-count: 1;
				animation-fill-mode: forwards;
			}

			.hidden {
				display: none !important;
			}

			.animate-spin {
				animation: spin 1s linear infinite;
			}
			@keyframes spin {
				from {
					transform: rotate(0deg);
				}
				to {
					transform: rotate(360deg);
				}
			}
		</style>
	</head>

	<body>
		<!-- Coral Reef Bottom Layer (Centered Medium Scale) -->
		<div class="coralreef-bottom-layer" id="coralReefLayer">
			<img src="/coralreef.png" alt="Coral Reef" class="coralreef-img" />
		</div>

		<!-- Tropical Swimming Fish Aquarium Stage (Random Spawn fish1-fish4) -->
		<div class="wbc-fish-aquarium-stage" id="fishAquariumStage" aria-hidden="true"></div>

		<!-- Aero Floating Glass Bubbles Stage (80% Back, 20% Front) -->
		<div class="aero-bubble-stage aero-stage-back" id="bubbleStageBack">
		</div>
		<div class="aero-bubble-stage aero-stage-front" id="bubbleStageFront">
		</div>

		<div class="page-wrap">
			<div class="bh-page">
				<!-- MAIN CONTENT CARD (Split Columns) -->
				<div class="bh-main-card">
					<!-- Top-Right RGB Bubble Toggle Button -->
					<button
						id="rgbBubbleToggleBtn"
						class="rgb-bubble-toggle-btn"
						title="Toggle Glass Bubbles"
					>
						<div class="rgb-bubble-inner"></div>
						<div class="ghost-bubble-outline"></div>
					</button>

					<div class="wbc-bubble-stage">
						<!-- Standalone Back Button Mini Glass Bubble (42px) -->
						<a
							href="/"
							class="wbc-back-bubble aero-component-bubble aero-float-4"
							id="backBtn"
							title="Back to Home"
						>
							<svg
								width="14"
								height="14"
								viewBox="0 0 14 14"
								fill="none"
								stroke="currentColor"
								stroke-width="2.5"
								stroke-linecap="round"
								stroke-linejoin="round"
							>
								<path d="M9 12L4 7l5-5"></path>
							</svg>
						</a>

						<!-- Header Title Orb (165px) -->
						<header class="bh-header aero-component-bubble aero-float-1">
							<h1 class="bh-title">
								<span class="title-nextwbc">NextWBC</span>
							</h1>
							<p class="bh-subtitle">
								Avian white blood cell classification using YOLO12.
							</p>
						</header>

						<!-- User Guide Orb (195px) -->
						<div class="wbc-guide aero-component-bubble aero-float-2">
							<h3 class="wbc-guide-title">How to Use</h3>
							<ul class="wbc-guide-list">
								<li>
									<span class="wbc-step-num">1</span>
									<span class="wbc-step-text"
										>Drag & drop or click image.</span
									>
								</li>
								<li>
									<span class="wbc-step-num">2</span>
									<span class="wbc-step-text"
										>YOLO12 AI detection pipeline.</span
									>
								</li>
								<li>
									<span class="wbc-step-num">3</span>
									<span class="wbc-step-text"
										>Review counts & cell crops.</span
									>
								</li>
							</ul>
							<div class="wbc-guide-note">
								JPG/PNG up to 20MB
							</div>
						</div>

						<!-- Quick Try Sample Orb (140px - Full Glass Photo Bubble) -->
						<div class="wbc-sample-section aero-component-bubble aero-float-3">
							<button
								id="wbcSampleBtn"
								class="wbc-sample-btn"
								title="Click to test sample image"
							>
								<img
									src="/raptor_wbc_00001.jpeg"
									alt="Sample Blood Smear"
									class="wbc-sample-bg-img"
								/>
								<div class="wbc-sample-overlay">
									<span class="wbc-sample-tag">QUICK TRY</span>
									<span class="wbc-sample-name">raptor_wbc.jpeg</span>
									<span class="wbc-sample-desc">Click to test</span>
								</div>
							</button>
						</div>

						<!-- GIANT CENTER MICROSCOPE ORB (380px) -->
						<main class="wbc-center-orb aero-component-bubble aero-float-1">
							<!-- Idle State: Upload Prompt -->
							<div id="wbcUploadPrompt" class="wbc-center-content">
								<div class="wbc-upload-zone" id="wbcDropzone" title="Click or drop blood smear image to upload">
									<input
										type="file"
										id="wbcFileInput"
										accept="image/*"
										class="hidden"
									/>
									<div class="wbc-upload-icon-wrapper">
										<svg
											xmlns="http://www.w3.org/2000/svg"
											width="44"
											height="44"
											viewBox="0 0 24 24"
											fill="none"
											stroke="currentColor"
											stroke-width="2.2"
											stroke-linecap="round"
											stroke-linejoin="round"
										>
											<path
												d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4M17 8l-5-5-5 5M12 3v12"
											></path>
										</svg>
									</div>
								</div>
							</div>

							<!-- Processing State: Progress Spinner -->
							<div
								id="wbcPreview"
								class="wbc-preview-container wbc-center-content hidden"
							>
								<!-- Pure CSS 3-Bubble Pop Inference Loader -->
								<div class="pl" aria-hidden="true">
									<div class="pl__bubble">
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
									</div>
									<div class="pl__bubble">
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
									</div>
									<div class="pl__bubble">
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
										<div class="pl__bubble-drop"></div>
									</div>
								</div>

								<img
									id="wbcPreviewImg"
									class="wbc-preview-img"
									src=""
									alt="Smear Preview"
								/>
								<div class="wbc-progress-bar-wrapper">
									<div
										class="wbc-progress-bar-fill"
										id="wbcProgressFill"
									>
									</div>
								</div>
								<div class="wbc-status-text" id="wbcStatusText">
									Connecting to model...
								</div>
							</div>

							<!-- Showing Result State: Annotated Detection Image Lens -->
							<div
								id="wbcResult"
								class="wbc-results-container wbc-center-content hidden"
							>
								<span
									class="wbc-panel-badge"
									id="panelBadge">Detection</span
								>
								<div class="wbc-image-wrapper">
									<img
										id="wbcAnnotatedImg"
										src=""
										alt="Annotated Detections"
										title="Click to toggle boxes"
									/>
								</div>
							</div>
						</main>

						<!-- Distribution Breakdown Orb (195px) -->
						<div class="wbc-chart-card aero-component-bubble aero-float-4">
							<h4 class="wbc-chart-title">
								Distribution
							</h4>
							<div id="wbcDistributionContainer">
								<!-- Dynamic Distribution Bars Injected Here -->
							</div>
						</div>

						<!-- Tiny Reset Button Orb (78px - Icon Only) -->
						<div class="wbc-reset-orb aero-component-bubble aero-float-3">
							<button
								id="wbcResetBtn"
								class="wbc-reset-btn"
								title="Upload New Image"
							>
								<svg
									xmlns="http://www.w3.org/2000/svg"
									width="24"
									height="24"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2.5"
									stroke-linecap="round"
									stroke-linejoin="round"
								>
									<path
										d="M21.5 2v6h-6M21.34 15.57a10 10 0 1 1-.57-8.38l5.67-5.67"
									></path>
								</svg>
							</button>
						</div>

						<!-- DECORATIVE MICRO GLASS BUBBLES (Subtle Aesthetic Accent) -->
						<div class="wbc-deco-bubble wbc-deco-1" aria-hidden="true"></div>
						<div class="wbc-deco-bubble wbc-deco-2" aria-hidden="true"></div>
						<div class="wbc-deco-bubble wbc-deco-3" aria-hidden="true"></div>
						<div class="wbc-deco-bubble wbc-deco-4" aria-hidden="true"></div>
						<div class="wbc-deco-bubble wbc-deco-5" aria-hidden="true"></div>
						<div class="wbc-deco-bubble wbc-deco-6" aria-hidden="true"></div>

						<!-- TIGHTLY CLUSTERED SATELLITE MOVING CELL CROP BUBBLES FIELD -->
						<div class="wbc-crops-cluster-field" id="wbcCropsGrid">
							<!-- Dynamic satellite crop glass bubbles float around center orb -->
						</div>

						<!-- Error Message Pane -->
						<div id="wbcError" class="wbc-error-msg hidden"></div>
					</div>
				</div>
			</div>
		</div>

		<!-- ── Page exit transition (stays inline — pure DOM, tiny) ── -->
		<script>
			const backBtn = document.getElementById("backBtn");
			if (backBtn) {
				backBtn.addEventListener("click", (e) => {
					e.preventDefault();
					document.body.classList.add("is-exiting");
					setTimeout(() => {
						window.location.href = backBtn.getAttribute("href");
					}, 280);
				});
			}
		</script>

		<!-- ── NextWBC Application (OOP Modules) ── -->
		<script>
			import { GradioSpaceClient } from "../lib/nextwbc/gradio-client";
			import { NextWbcUI } from "../lib/nextwbc/nextwbc-ui";
			import { AeroBubbleEngine } from "../lib/nextwbc/bubble-engine";
			import { FishSpawner } from "../lib/nextwbc/fish-spawner";

			// ── Initialize the Gradio API client & UI controller ──
			const client = new GradioSpaceClient();
			const ui = new NextWbcUI(client);
			ui.init();

			// ── Initialize the Aero Bubble Physics Engine & Fish Spawner ──
			function initUnderwaterWorld() {
				// 1. Aero Bubbles
				const stageBack = document.getElementById("bubbleStageBack");
				const stageFront = document.getElementById("bubbleStageFront");
				if (stageBack && stageFront) {
					const engine = new AeroBubbleEngine(stageBack, stageFront, {
						coralReefLayer: document.getElementById("coralReefLayer"),
						toggleBtn: document.getElementById("rgbBubbleToggleBtn"),
						config: {
							totalBubbles: 10,
							minSize: 18,
							maxSize: 46,
							backRatio: 0.75,
							mouseRepulsionDist: 140,
							mouseRepulsionForce: 2.2,
						},
					});
					engine.start();
				}

				// 2. Random Fish Spawner (fish1 - fish4)
				const fishContainer = document.getElementById("fishAquariumStage");
				if (fishContainer) {
					const spawner = new FishSpawner(fishContainer, { maxFish: 4 });
					spawner.start();
				}
			}

			if (document.readyState === "loading") {
				document.addEventListener("DOMContentLoaded", initUnderwaterWorld);
			} else {
				initUnderwaterWorld();
			}
		</script>
		<Watermark />
	</body>
</html>
